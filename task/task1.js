//Task 1. Unique Caption
let unique = arr => Array.from(new Set(arr));

/*let unique = arr => {
  let set = new Set(arr),
      returnArr = [];
  set.forEach( value => returnArr.push(value) );
  return returnArr; 
};*/
//done + ± +
