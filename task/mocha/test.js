describe("Task 1. (Task Name)", function() {
  it("Description", function() {
    assert.equal(testTask1(), 1);
  });
});

describe("Task 2. (Task Name)", function() {
  it("Description", function() {
    assert.equal(testTask2(), 2);
  });
});

describe("Task 3. (Task Name)", function() {
  it("Description", function() {
    assert.equal(2, 3);
  });
});

describe("Task 4. (Task Name)", function() {
  it("Description", function() {
    assert.equal(2, 1);
  });
});

